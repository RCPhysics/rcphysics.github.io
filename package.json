{
  "name": "rcphysics-site",
  "version": "1.0.0",
  "description": "Personal & lab site for Richard C. Prince, PhD — built with Eleventy.",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --port=8080",
    "build": "eleventy",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "js-yaml": "^4.1.0"
  },
  "dependencies": {
    "js-yaml": "^4.1.1"
  }
}
